# PostgreSQL Cloud Database Configuration
# Get these credentials from your cloud provider (Supabase, Railway, Render, etc)

# Database connection string (preferred method for cloud databases)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@your-host.com:5432/your_database

# OR individual connection settings
DB_HOST=your-host.com
DB_PORT=5432
DB_NAME=your_database
DB_USER=postgres_user
DB_PASSWORD=your_password

# Server port
PORT=3003

# Email configuration (optional)
SMTP_HOST=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_USER=your_email@outlook.com
SMTP_PASS=your_password
